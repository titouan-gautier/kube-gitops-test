apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-test-gitops.fullname" . }}
  labels:
    {{- include "kube-test-gitops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-test-gitops.selectorLabels" . | nindent 4 }}
